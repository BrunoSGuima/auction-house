<h1>Faça uma nova pergunta</h1>
<%= form_with(model: [@auction_lot, @question]) do |form| %>
  <div>
    <%= form.label :question_text %>
    <%= form.text_area :question_text %>
  </div>
  <div>
    <%= form.submit "Enviar", class: 'btn btn-primary btn-bus' %>
  </div>
<% end %>

<%= link_to 'Voltar', root_path, class: 'btn btn-warning btn-bid' %>
